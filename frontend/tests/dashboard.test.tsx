import {render,screen} from "@testing-library/react"; import {describe,it,expect} from "vitest"; import {Evidence} from "@/components/dashboard"; describe("operations views",()=>{it("renders battery evidence",()=>{render(<Evidence kind="battery"/>);expect(screen.getByText("Physics-regularized MLP ensemble")).toBeTruthy()});it("renders system boundary",()=>{render(<Evidence kind="system"/>);expect(screen.getByText(/Not a BMS/)).toBeTruthy()})});
