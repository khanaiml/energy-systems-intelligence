# Executed battery evidence

This directory is a selected, unmodified copy of the executed notebook outputs: benchmark JSON/Markdown, all result tables, and all generated figures. The notebook is copied under `notebooks/`. The original parent assets remain the immutable source.
